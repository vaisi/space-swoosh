<?xml version="1.0" encoding="utf-8" ?>
<!-- AndroidManifest.xml — Capacitor host. Changes: disable Firebase Analytics Advertising ID collection (gameplay-only telemetry). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- The game is a fixed 2:3 portrait surface; landscape would letterbox
             it and rebuild the canvas mid-run. -->
        <!-- adjustResize: shrink the WebView above the soft keyboard so the
             Submit Signal call-sign field stays inside the modal (adjustPan
             was panning the page and desyncing the DOM input from the canvas). -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Gameplay analytics only — no Advertising ID (Play Data Safety). -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Wall-boop haptic tick (@capacitor/haptics also declares this; keep
         explicit so a merge miss never silently drops vibration). -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
